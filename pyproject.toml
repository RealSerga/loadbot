[project]
name = "loadbot"
version = "0.1.0"
description = "Простой Telegram-бот, который обновляет одно сообщение с сетевой статистикой"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiogram>=3.4,<4.0",
    "psutil>=5.9",
]


